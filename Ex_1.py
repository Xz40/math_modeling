class school_journal:
    def __init__(self):
        a.subject = subject
        b.student = student
        c.grade_list = grade_list
    def grade_list(grade):
        while grade != 0:
           c.grade_list = grade
     def printer(a.subject, b.student)
        print(f'name = {b.student} \n grade list = {c.grade_list}')

